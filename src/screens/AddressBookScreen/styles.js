import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        paddingVertical: 10,
        paddingHorizontal: 5,
    },
    isEmptyLayout: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
});
