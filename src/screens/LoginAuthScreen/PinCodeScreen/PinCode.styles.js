import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    textLayout: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    text: {
        textAlign: 'center',
        fontSize: 35,
        fontWeight: 'bold',
    },
    pinLayout: { flex: 2, alignItems: 'center', justifyContent: 'center' },
});
