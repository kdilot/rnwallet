import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%',
    },
    footerText: {
        zIndex: 1,
        position: 'absolute',
        bottom: 30,
        fontSize: 10,
    },
});
