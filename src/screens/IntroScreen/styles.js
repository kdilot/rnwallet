import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    ContainerView: {
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%',
    },
    FooterText: {
        zIndex: 1,
        position: 'absolute',
        bottom: 30,
        fontSize: 10,
    },
});
