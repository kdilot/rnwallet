import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
    },
    scannerLayout: {
        flex: 3,
    },
    textLayout: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1,
    },
    text: {
        fontSize: 15,
        textAlign: 'center',
    },
});
